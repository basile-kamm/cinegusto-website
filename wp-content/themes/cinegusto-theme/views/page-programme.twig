{% extends "base.twig" %}

{% set days = get_f %}

{% block content %}
	<section class="banner h-[60vh] flex items-center justify-center mx-10">
		{% embed 'components/card.twig'  with { padding_outside: ' p-2 md:p-3'} %}
			{% block content %}
				<div class="flex flex-col gap-4 items-center text-center">
					<h1 class="text-3xl">Programme</h1>
					<p class="text-l">Du 8 au 11 septembre 2027</p>
					<p class="text-l">Bulle - La-Tour-de-Trême</p>
				</div>
			{% endblock %}
		{% endembed %}
	</section>
	{% for thematique in thematiques %}
		<section class="grid items-start pb-50 {{ thematique.theme_id }}">
			{% embed 'components/card.twig' with { is_border_double: true, container_class: 'lg:col-span-6 ' } %}
				{% block content %}
					<div class="flex flex-col gap-8 lg:gap-10">
						<h2 class="text-xl">{{ thematique.theme_title }}</h2>
						<p class="text-base">{{ thematique.theme_description }}</p>
						<h3 class="text-xl font-normal">Menu du jour</h3>
						<div class="flex flex-col gap-4">
							<p class="text-l">18H - Parvis de la salle CO2</p>
							<p>Cuisiné par
								<span class="text-l">{{ thematique.food_details.cooked_by }}</span>
							</p>
							<p>Menu inspriré du film
								<span class="text-l">{{ thematique.food_details.inspired_by }}</span>
							</p>
						</div>
					</div>
					<div class="flex flex-col gap-2 mt-12 lg:mt-14">
						<h4 class="text-l">Plat principal revisité</h4>
						{% include 'components/accordion.twig' with { title: thematique.food_details.main_menu.title, text: thematique.food_details.main_menu.details, is_font_medium: false } %}
						<h4 class="text-l">Option végétarienne/vegan</h4>
						{% include 'components/accordion.twig' with { title: thematique.food_details.vegan_menu.title, text: thematique.food_details.vegan_menu.details, is_font_medium: false } %}
					</div>
				{% endblock %}
			{% endembed %}
			{% embed 'components/card.twig' with { is_border_double: true, container_class: 'col-span-5 lg:col-start-7' } %}
				{% block content %}
					<div class="flex flex-col gap-8 lg:gap-10">
						<h2 class="text-xl font-normal">{{ thematique.theme_title }}</h2>
						<p class="text-base">{{ thematique.theme_description }}</p>
						<a href="/programme" class="text-l underline lg:hidden">Voir ce qui mijote</a>
					</div>
				{% endblock %}
			{% endembed %}
		</section>
	{% endfor %}
	<section class="relative spiked">
		<div class="background bg-creme"></div>
		<div class="background image opacity-10 mix-blend-plus-darker"></div>

		<div class="grid pt-50 pb-30">
			{% include 'components/vod.twig' %}
		</div>
	</section>
{% endblock %}
